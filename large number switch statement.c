/// Write a C program to find the largest of three numbers.

#include <stdio.h>
int main ()
{
    int a,b,c;
    printf("Enter Three Number :");
    scanf("%d %d %d",&a,&b,&c);


switch(a>b&&a>c){
     case 0:
    printf("%d is larger than three Number",a);
break;


    switch(b>a&&b>c):
       case 1: printf("%d is large than three Number",b);

    break;

    default:
        printf("%d is large than three Number",c);



    getch ();

}

