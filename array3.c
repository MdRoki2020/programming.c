#include <stdio.h>
int main()
{
    int x[10],n,i;


    printf("how many number: ");
    scanf("%d",&n);

    for(i=0; i<n; i++);
    scanf("%d",&x[i]);

    int max=x[0];
    if(max < x[i]);
    max= x[i];



    printf("max : %d\n",max);

    return 0;

}
